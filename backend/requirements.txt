flask==3.1.3
flask-cors==6.0.0
joblib==1.4.2
numpy==2.4.3
pandas==2.2.2
scikit-learn==1.8.0
librosa==0.10.2
soundfile==0.12.1
# Voice-activity detection: rejects game audio / music / background noise before the
# voice-emotion model sees it (an energy floor alone can't). Optional at runtime —
# the backend degrades gracefully if this isn't installed.
webrtcvad-wheels==2.0.14
fpdf2==2.7.9
gunicorn==22.0.0
shap==0.51.0
flask-limiter==3.8.0
sentry-sdk[flask]==2.14.0
psycopg2-binary==2.9.9
firebase-admin==6.5.0
pytest==7.4.3
