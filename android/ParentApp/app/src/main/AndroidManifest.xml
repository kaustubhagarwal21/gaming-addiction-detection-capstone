<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android">

    <!-- Network -->
    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />

    <!-- Notifications for alerts -->
    <uses-permission android:name="android.permission.POST_NOTIFICATIONS" />

    <!-- Keep polling service alive -->
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE" />
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE_DATA_SYNC" />
    <uses-permission android:name="android.permission.RECEIVE_BOOT_COMPLETED" />

    <application
        android:name=".ParentApp"
        android:allowBackup="false"
        android:icon="@drawable/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@drawable/ic_launcher"
        android:supportsRtl="true"
        android:theme="@style/Theme.ParentApp"
        android:networkSecurityConfig="@xml/network_security_config">

        <activity
            android:name=".activities.SplashActivity"
            android:exported="true"
            android:theme="@style/Theme.ParentApp.Splash">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <activity
            android:name=".activities.LoginActivity"
            android:exported="false"
            android:theme="@style/Theme.ParentApp.NoActionBar" />

        <activity
            android:name=".activities.ParentalDashboardActivity"
            android:exported="false"
            android:theme="@style/Theme.ParentApp.NoActionBar" />

        <activity
            android:name=".activities.AlertsActivity"
            android:exported="false"
            android:theme="@style/Theme.ParentApp.NoActionBar" />

        <activity
            android:name=".activities.RecommendationsActivity"
            android:exported="false"
            android:theme="@style/Theme.ParentApp.NoActionBar" />

        <activity
            android:name=".activities.SettingsActivity"
            android:exported="false"
            android:theme="@style/Theme.ParentApp.NoActionBar" />

        <activity
            android:name=".activities.EmotionInsightsActivity"
            android:exported="false"
            android:theme="@style/Theme.ParentApp.NoActionBar" />

        <activity
            android:name=".activities.ChatAnalysisActivity"
            android:exported="false"
            android:theme="@style/Theme.ParentApp.NoActionBar" />

        <activity
            android:name=".activities.WeeklyReportActivity"
            android:exported="false"
            android:theme="@style/Theme.ParentApp.NoActionBar" />

        <activity
            android:name=".activities.ChildSelectActivity"
            android:exported="false"
            android:theme="@style/Theme.ParentApp.NoActionBar" />

        <!-- Background polling for alerts -->
        <service
            android:name=".service.AlertPollingService"
            android:exported="false"
            android:foregroundServiceType="dataSync" />

        <!-- FCM push notification receiver -->
        <service
            android:name=".service.ParentFCMService"
            android:exported="false">
            <intent-filter>
                <action android:name="com.google.firebase.MESSAGING_EVENT" />
            </intent-filter>
        </service>

        <!-- When the app is backgrounded/killed, the SYSTEM (not our code) posts FCM
             notification messages, and on Android 8+ it needs a channel. Point it at our
             high-importance alerts channel (created in ParentApp.onCreate) so background
             high-risk pushes show with the right priority instead of a fallback channel. -->
        <meta-data
            android:name="com.google.firebase.messaging.default_notification_channel_id"
            android:value="alerts_channel" />
        <meta-data
            android:name="com.google.firebase.messaging.default_notification_icon"
            android:resource="@drawable/ic_alert" />

        <!-- FileProvider for PDF sharing -->
        <provider
            android:name="androidx.core.content.FileProvider"
            android:authorities="${applicationId}.fileprovider"
            android:exported="false"
            android:grantUriPermissions="true">
            <meta-data
                android:name="android.support.FILE_PROVIDER_PATHS"
                android:resource="@xml/provider_paths" />
        </provider>

        <!-- Boot receiver -->
        <receiver
            android:name=".service.BootReceiver"
            android:exported="false">
            <intent-filter>
                <action android:name="android.intent.action.BOOT_COMPLETED" />
            </intent-filter>
        </receiver>

    </application>

</manifest>
